[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so React Router client routes (/modulos/*, /hatlas-agent) resolve.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
